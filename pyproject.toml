[project]
name = "pgramham"
version = "0.1.0"
description = "Paul Graham's essays organized by topics"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "markdownify>=0.14",
    "rich>=13.0",
]

[project.scripts]
scrape = "src.scraper:main"
